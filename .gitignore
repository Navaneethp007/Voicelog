# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.venv/
venv/
.env


# Pytest
.pytest_cache/

# voicelog keeps its cache and run watermark in .git/voicelog/, outside the
# work tree, so nothing needs ignoring. This covers the pre-0.3 location,
# for anyone whose clone still has the old file lying around.
.changelog/.voicelog-cache.json

# OS
Thumbs.db
.DS_Store
